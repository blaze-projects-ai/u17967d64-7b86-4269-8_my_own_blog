<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof pageTitle !== 'undefined' ? pageTitle + ' — ' : '' %>My Own Blog</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <header class="site-header">
    <div class="container">
      <a href="/" class="site-title">My Own Blog</a>
      <nav class="site-nav">
        <a href="/">Home</a>
        <% categories.forEach(cat => { %>
          <a href="/category/<%= cat %>"<%= typeof currentCategory !== 'undefined' && currentCategory === cat ? ' class="active"' : '' %>><%= cat %></a>
        <% }) %>
      </nav>
    </div>
  </header>
  <main class="container">
